# Upload Versions

Each import/upload saved by the analyzer gets one version note here.

Use these notes to trace:

- which files were uploaded
- when the data was captured
- how many ads/pages were in that version
- where the raw JSON version is stored
- what changed compared with another upload
